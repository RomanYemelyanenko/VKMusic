<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="70dp"
    android:layout_width="match_parent"
    android:orientation="horizontal">
    <LinearLayout
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:layout_weight="1">

        <EditText style="@style/EditTextStyle"
            android:id="@+id/user_id"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:background="@drawable/rounded_left_top_shape"
            android:hint="@string/id"/>
        <EditText style="@style/EditTextStyle"
            android:id="@+id/password"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:layout_marginTop="-2dp"
            android:hint="@string/password"
            android:password="true"
            android:background="@drawable/rounden_left_bottom_shape"/>
    </LinearLayout>
    <Button style="@style/TextStyle"
        android:id="@+id/login_button"
        android:layout_height="match_parent"
        android:layout_width="wrap_content"
        android:layout_weight="0"
        android:singleLine="true"
        android:layout_marginLeft="-2dp"
        android:background="@drawable/rounded_right_shape"
        android:text="@string/login"/>
</LinearLayout>

